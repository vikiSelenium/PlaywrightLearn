const browserVersion= "chrome10"// Global  variable

function getBrowserVersion(){

    if(browserVersion === "chrome10")

    {
      var browserVersion="chrome11";//local variable
      varbrowserVersion="chrome12";//local variable
  console.log(browserVersion);
    }
    console.log(browserVersion);
 
   }
getBrowserVersion();    